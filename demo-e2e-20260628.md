# GitReview End-to-End Demo — 2026-06-28

## What was tested

Full webhook flow from simulated `pull_request.opened` event to AI-generated PR comment, using the GitReview prototype and a GitHub personal access token fallback.

## Test setup

- Repository: [Mingyue-ai/gitreview-testbed](https://github.com/Mingyue-ai/gitreview-testbed)
- Pull request: [#1 Add greet function](https://github.com/Mingyue-ai/gitreview-testbed/pull/1)
- Prototype: [Mingyue-ai/gitreview-prototype](https://github.com/Mingyue-ai/gitreview-prototype)
- Server: running locally on port 3000 with `.env`:
  - `GITHUB_TOKEN` from authenticated `gh` CLI
  - `ANTHROPIC_API_KEY` from workspace environment
  - `GITHUB_WEBHOOK_SECRET=demo-secret-2026`

## Trigger

Simulated GitHub webhook POST to `/webhooks/api/github/webhooks` with:
- `X-GitHub-Event: pull_request`
- `X-GitHub-Delivery: demo-delivery-...`
- `X-Hub-Signature-256: sha256=...`
- Payload: `action: opened`, PR #1 in `Mingyue-ai/gitreview-testbed`

## Result

Server returned `200 OK`. A comment was posted on PR #1:

## AI-generated PR description

## Summary
Adds a new `greet` function to a new `demo.js` file that returns a formatted greeting string.

## What Changed
- Created `demo.js` with a simple `greet(name)` function using template literals
- Exported the function as a CommonJS module

## Why
Test/demo change to validate GitReview PR description generation workflow. Not intended for production use.

## Risks & Notes
- **No input validation** on `name` parameter—passing `undefined`/`null` will produce malformed output (`"Hello, undefined!"`)
- **No tests included** for this change
- **No TypeScript types**—consider adding JSDoc or `.d.ts` if this becomes real code
- Consider using ESM (`export` syntax) instead of CommonJS for consistency with modern Node.js projects

## Follow-ups
- [ ] Add unit tests if this code is retained
- [ ] Remove or relocate if purely for GitReview testing

---
_Generated by [GitReview](https://github.com/apps/gitreview)_

## Verification

- `GET /health` returns `{"status":"ok","version":"0.1.0"}`
- Webhook signature verified by `@octokit/webhooks`
- Diff fetched via GitHub API (`mediaType: diff`)
- AI description generated by Claude (`claude-sonnet-4-6`)
- Comment posted via GitHub Issues API

## Notes

- This run used the PAT fallback (`GITHUB_TOKEN`) instead of GitHub App credentials, proving the prototype works before a formal GitHub App is registered.
- For production use, register a GitHub App and switch to `GITHUB_APP_ID` + `GITHUB_PRIVATE_KEY` so the app can be installed by customers.
