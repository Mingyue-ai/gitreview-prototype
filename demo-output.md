Running GitReview local demo...

## AI-generated PR description

## Summary
Adds a `/health` endpoint and hardens diff truncation logic ahead of initial deployment.

## Changes

### Added
- **`GET /health`** endpoint in `src/index.ts` — returns `{"status":"ok","version":"0.1.0"}` for load balancer and monitoring readiness checks

### Fixed
- **Diff truncation edge case** in `src/claude.ts` — handles diffs with no newlines within the first `MAX_DIFF_CHARS` characters, preventing `lastIndexOf` returning `-1` and producing malformed output

### Dependencies
- Added `@octokit/rest@^20.1.0` to `package.json`

## Why this matters
- **Health checks** are required for any production deployment with a load balancer or orchestrator (e.g., Kubernetes, ECS)
- **Truncation fix** prevents sending malformed diff context to Claude API, which could cause parsing errors or degraded review quality on large single-line files (e.g., minified JS, generated lockfiles)

## Risks & follow-ups
- **Version string is hardcoded** (`"0.1.0"`) — consider pulling from `package.json` or `process.env` to avoid drift
- **Health check is minimal** — does not verify downstream dependencies (GitHub API, Anthropic API); consider adding dependency health checks if SLAs require it
- **No tests included** for the truncation fix or health endpoint — recommend adding before next release
- Verify `@octokit/rest` addition is intentional; the diff doesn't show usage in this PR — may be for a follow-up or could be leftover from another branch

---
_Generated by [GitReview](https://github.com/apps/gitreview)_
