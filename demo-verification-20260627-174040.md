Running GitReview local demo...

## AI-generated PR description

## Add health check and harden diff truncation

### What changed
- **Added `/health` endpoint**: Simple HTTP health check returning `{"status": "ok", "version": "0.1.0"}` for deployment monitoring and load balancer readiness probes.
- **Fixed diff truncation edge case**: `truncateDiff()` now handles diffs with no newlines within the first `MAX_DIFF_CHARS`, preventing `slice(0, -1)` from producing malformed output when `lastIndexOf("\n")` returns `-1`.
- **Added `@octokit/rest` dependency**: Required for upcoming GitHub API interactions (not yet used in this PR).

### Why it matters
The health endpoint unblocks production deployment infrastructure that requires a readiness probe. The truncation fix prevents a subtle bug where newline-free diffs (e.g., minified files, single-line changes) would be sliced incorrectly, potentially corrupting the prompt sent to Claude.

### Risks / follow-ups
- **Version string is hardcoded** (`"0.1.0"`); consider reading from `package.json` or environment to avoid drift.
- `@octokit/rest` is added but unused—ensure the follow-up PR that consumes it lands soon to avoid dependency bloat.
- Health check has no dependency verification (e.g., GitHub API reachability, Claude API token validity); consider deep health checks if production SLOs require it.

---
_Generated by [GitReview](https://github.com/apps/gitreview)_
